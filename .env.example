# Copy into .env.local

# mock (default) | jev
MODEL=mock

# Required only when MODEL=jev
# AI_GATEWAY_API_KEY=
# TYPESAFE_AI_API_KEY=

# Market data is Yahoo Finance. No quote API key.

# Static GitHub Pages builds post opens to this origin (defaults to jev.qinvesting.ai).
# NEXT_PUBLIC_VIEWS_ORIGIN=https://jev.qinvesting.ai
